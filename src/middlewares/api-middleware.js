// Imports
import { ROOT_ROUTES } from '@/constants/routes/root';
import { AUTH_ROUTES } from '@/constants/routes/auth';
import { NextResponse } from 'next/server';

// Api middleware function 
export async function apiRoutesMiddleware(request) {
  console.log('--------------------------------------------------------------------');
}